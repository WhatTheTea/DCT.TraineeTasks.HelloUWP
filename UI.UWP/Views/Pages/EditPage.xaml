<Page
    x:Class="DCT.TraineeTasks.HelloUWP.UI.UWP.Views.Pages.EditPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DCT.TraineeTasks.HelloUWP.UI.UWP.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <TextBlock Text="{x:Bind ViewModel.FirstName, Mode=OneWay}"
           Grid.Column="0"/>
        <TextBlock Text="{x:Bind ViewModel.LastName, Mode=OneWay}"
           Grid.Column="1"/>
        <TextBlock Text="{x:Bind ViewModel.Entries[0].Text, Mode=OneWay}"
           Grid.Column="2"/>
    </StackPanel>
</Page>
