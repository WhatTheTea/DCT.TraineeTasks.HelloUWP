<Page
    x:Class="DCT.TraineeTasks.HelloUWP.UI.UWP.Views.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DCT.TraineeTasks.HelloUWP.UI.UWP.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >
    
    <StackPanel>
        <Button Content="Add person" Command="{x:Bind ViewModel.AddPersonCommand}"/>

        <ListView x:Name="PeopleListView"
                  ItemsSource="{x:Bind ViewModel.People, Mode=OneWay}"
                  SelectionChanged="PeopleListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                            <ColumnDefinition Width="100" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding FirstName, Mode=OneTime}"
                                   Grid.Column="0"/>
                        <TextBlock Text="{Binding LastName, Mode=OneTime}"
                                   Grid.Column="1"/>
                        <TextBlock Text="{Binding Entries[0].Text}"
                                   Grid.Column="2"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


    </StackPanel>
</Page>
